// HttpServer is http.*'s type; a program cannot also define one.
#include <std/network>
#include <std/io>

struct HttpServer {
    port: int;
}

fn main() {
    let s = http.localhost();
    io.println(s.ok());
}
