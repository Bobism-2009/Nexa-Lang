// HttpResponse is http.*'s type; a program cannot also define one.
#include <std/network>
#include <std/io>

struct HttpResponse {
    status: int;
}

fn main() {
    let r = http.get("http://example.com/");
    io.println(r.ok());
}
