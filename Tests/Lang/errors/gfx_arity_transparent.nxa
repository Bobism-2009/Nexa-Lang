// gfx.transparent is a getter and a setter in one name, the way the other two
// of the overlay trio are: a window is see-through or it is not, and there is
// no second thing to say about it.
#include <std/gfx>
fn main() {
    let t: int = gfx.transparent(1, 2);
}
