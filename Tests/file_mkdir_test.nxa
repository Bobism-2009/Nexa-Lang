#include <std/file>
#include <std/io>

fn main() {
    file.mkdir("Tests/_mkdir_test/nested");
    if (file.exists("Tests/_mkdir_test/nested") == 1) {
        io.println("ok");
    } else {
        io.println("fail");
    }
}
